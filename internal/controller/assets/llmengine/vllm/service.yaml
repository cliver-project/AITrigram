apiVersion: v1
kind: Service
metadata:
  name: placeholder
  labels:
    app: placeholder
    model: placeholder
spec:
  type: ClusterIP
  selector:
    app: placeholder
    model: placeholder
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
